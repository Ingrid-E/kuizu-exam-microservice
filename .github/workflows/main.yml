name: Phyton testing
on:
  push:
    branches:
      - main
      - develop
      - feature/tests
  pull_request:
    branches:
      -main
      -develop

jobs:
  build:
    name: Build 🧪
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: gradle/gradle-build-action@v2
        with:
          java-version: '17.0.4'
          distribution: 'adopt'
      - run: ./gradlew build
      - run: grandle test